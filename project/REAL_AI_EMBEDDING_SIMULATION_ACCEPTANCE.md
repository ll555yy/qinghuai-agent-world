# 真实 AI、Embedding 与七日模拟验收清单

状态：已完成。

## 自动化验收

- [x] 六个文本协议的离线契约测试通过。
- [x] 真实六协议验收脚本只有显式 `--live` 才联网。
- [x] 真实探测确认 Agent Plan 返回 2048 维；代码和测试严格拒绝其他维度。
- [x] 新 Memory 可生成向量；失败时保持 `NULL`。
- [x] 普通 Run 保存不会删除内容未变化的向量。
- [x] 回填重复运行能跳过已完成记录，模型变化时会重新生成。
- [x] 混合检索命中正确 owner 的相关 Memory，不能召回其他 NPC 私有 Memory。
- [x] 25 条有向关系均有私有开局 Memory 覆盖，并由场景加载器校验。
- [x] 七日模拟器有调用、消息、时间上限，且可用 Fake 模型离线测试。
- [x] 全量单元、集成、Ruff、Mypy 和敏感信息扫描通过。

## 真实运行证据

- [x] 方舟文本模型使用已轮换、本地配置的密钥完成连通。
- [x] 六协议真实验收已完成：六类全部首轮 Schema 成功，无格式重试。
- [x] 真实 Embedding 返回 2048 维；迁移、36 条私有 Memory 回填、幂等重跑与 Repository 重启复验完成。
- [x] `observer` 七日模拟完成并生成 JSON/Markdown 安全报告。
- [x] `pro_lin` 七日模拟完成并生成 JSON/Markdown 安全报告。
- [x] `pro_zhao` 七日模拟完成并生成 JSON/Markdown 安全报告。
- [x] 根据真实报告形成调参记录，并复跑所有受影响路线。

## 前端门禁

上述真实运行证据与自动化验收均已完成，正式前端开发门禁解除。三条合格样本的总体结局均为 `no_submission`；pro_lin / pro_zhao 的玩家任务均为 `failed`，但各自产生了由本人台词证明的 `support` 立场，证明玩家影响链路有效且不会被脚本保证胜利。
