# 前端开发前后端验收报告

状态：已完成，前端门禁已解除。

## 已通过

- 方舟 `doubao-seed-2.0-lite` 六协议真实调用全部首轮 Schema 成功。
- Agent Plan `doubao-embedding-vision-251215` 真实返回 2048 维。
- 开发库和测试库已迁移到 `vector(2048)`，使用 `halfvec(2048)` HNSW 表达式索引；Alembic check 无漂移。
- 指定 Run 的 36 条私有 Memory 已在用户授权下完成真实回填；重跑全部跳过，Repository 重开后语义召回仍生效，owner 越界为 0。
- 方舟私有 Memory 批次经实测固定为 8；36 条单批失败不会修改权威 Memory，8 条分批全部成功。
- 25 条有向初始关系均有 owner 私有 `scenario_seed` Memory，并由加载器强制校验。
- 一次真实 Day1 聊天闭环已到达 18:00：5 次 NPC 日思考、3 次 NPC 邀请、2 场聊天、玩家成功加入并发言、34 条 NPC 台词、4 次离场沉淀；2 条会话 Memory 入库，38/38 Memory 均有真实向量，Repository 重开后保持一致，临时 Run 已删除。
- 该基线产生 90 次逻辑调用、94 次物理请求并耗时 876926 ms；据此把单触发连续 NPC 回复深度从 8 降为 2、参与者变化追加深度从 7 降为 1。三路线 Fake 模型预检仍全部到达 Day7 18:00。
- 正式时间比例已按用户确认调整为完整世界日 20 分钟：`world/step` 每 2 个现实有效秒推进 1 个世界分钟；Day1 18 分钟，七日约 138 分钟。模拟使用同一换算但可批量提交现实秒立即快进。
- 普通 pytest 强制内存仓储和空 Key，不能因本机 `.env` 产生真实请求。
- 三条真实七日路线均到达 Day7 18:00、触发 7/7 世界事件、完成 Repository 重启恢复并删除临时 Run：

| 路线 | 对话/消息 | 玩家消息 | Memory 召回 | 向量命中 | 明确立场 | 结局 |
|---|---:|---:|---:|---:|---|---|
| observer | 14/45 | 0 | 2 | 16 | 0 | `no_submission` |
| pro_lin | 12/44 | 2 | 1 | 8 | `npc_001=support` | `no_submission` |
| pro_zhao | 14/51 | 2 | 1 | 7 | `npc_003=support` | `no_submission` |

- 三条路线均使用真实方舟文本模型与真实 Embedding；Graph 扩展命中为 0，表示向量种子已直接满足本次查询，不影响 owner-safe 混合检索验收。
- Prompt/玩法参数已依据真实样本完成调优并复跑：连续回复链为 2、参与者变化链为 1、初始 Memory cache 为 1；支持路线采用 Day1 调查旧事、Day7 明确询问最终立场的两阶段玩家输入。
- 最终自动化验收：D-066 上下文压缩更新后为 158 passed（含 PostgreSQL 集成测试）、Ruff 全范围通过、mypy 61 个源文件通过；开发库与测试库均在 Alembic head `b18e9f0a1234`。

## 非阻塞边界

- 三条合格样本的总体结局均为 `no_submission`，支持路线玩家任务均为 `failed`。这不是验收失败：玩家已真实改变单人立场，但脚本没有强制其他 NPC 或周慎之配合，整体胜利仍需实际游玩中继续聊天推动。
- 方舟或 Embedding 短时不可用时，Memory 正文先落库、向量待幂等 backfill；真实路线已验证这种降级不会污染权威状态。
- React/Phaser 前端尚未开发。

后端前置目标已经完成，可以开始正式前端开发。
