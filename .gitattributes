# The regression baseline was checksummed with CRLF bytes. Force the same
# checkout representation on every OS so its SHA-256 stays reproducible.
project/evaluation-results/live-baseline-2026-08-23/* text eol=crlf
