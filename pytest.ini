[pytest]
# Pytest 9 creates atomic cache staging directories beside .pytest_cache.
# The managed Windows sandbox can leave those directories with unreadable ACLs,
# so disable only pytest's cache provider; test collection and tmp_path remain.
addopts = -p no:cacheprovider
